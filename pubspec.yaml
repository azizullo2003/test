name: merchant_kyc_app

description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  easy_localization: 3.0.2
  intl: ^0.18.0
  flutter_native_splash: ^2.3.1
  bottom_sheet_scaffold: ^0.1.8
  flutter_bloc: ^8.1.3
  provider: ^6.0.5
  animated_toggle_switch: ^0.6.2
  animated_custom_dropdown: ^1.5.0
  flutter_expanded_tile: ^0.3.7
  swipeable_page_route: ^0.3.0
  email_validator: ^2.1.16
  intl_phone_field: ^3.1.0
  dots_indicator:


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/translations/